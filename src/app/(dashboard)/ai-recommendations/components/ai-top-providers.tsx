"use client";
import { Crown } from "lucide-react"; import { Card } from "@/components/ui/card";
type Provider={providerId:string;businessName:string;category:string;city:string;recommendationCount:number;avgScore:number;avgConfidence:number;avgDistance:number};
export function AiTopProviders({isLoading,topProviders}:{isLoading:boolean;topProviders:Provider[]}) {
 return <Card className="overflow-hidden"><div className="border-b border-border/30 p-4"><h3 className="flex items-center gap-2 text-sm font-bold text-white"><Crown className="h-4 w-4 text-amber-400"/> أكثر المزودين ترشيحا</h3></div><div className="overflow-x-auto"><table className="w-full text-right text-xs"><thead className="bg-secondary/20 text-muted-foreground"><tr>{["المزود","التصنيف","المدينة","الترشيحات","درجة المطابقة","الثقة","المسافة"].map(h=><th className="px-4 py-3" key={h}>{h}</th>)}</tr></thead><tbody>{isLoading?<tr><td colSpan={7} className="p-6 text-center text-muted-foreground">جاري التحميل...</td></tr>:topProviders.map(x=><tr className="border-t border-border/20" key={x.providerId}><td className="px-4 py-3 font-bold text-white">{x.businessName}</td><td className="px-4 py-3">{x.category}</td><td className="px-4 py-3">{x.city}</td><td className="px-4 py-3">{x.recommendationCount}</td><td className="px-4 py-3 text-emerald-400">{x.avgScore.toFixed(1)}%</td><td className="px-4 py-3 text-amber-400">{(x.avgConfidence*100).toFixed(1)}%</td><td className="px-4 py-3">{x.avgDistance.toFixed(1)} كم</td></tr>)}</tbody></table></div></Card>;
}
